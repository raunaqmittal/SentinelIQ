# SentinelIQ backend.
#
# The UI is NOT here — Streamlit needs pandas, and pandas imported after torch
# crashes the interpreter (see Docs/PROGRESS.md, 2026-08-16). The dashboard has
# its own frontend/requirements.txt and its own environment.

# --- ingestion -------------------------------------------------------------
beautifulsoup4>=4.12
pymupdf>=1.24
python-docx>=1.1

# --- retrieval (FROZEN — pinned exactly, see "FINAL RETRIEVAL DECISION") ----
# These versions are the ones every recorded measurement was taken with.
# Changing any of them invalidates the frozen benchmark.
torch==2.13.0
sentence-transformers==5.7.0
transformers==5.15.0
faiss-cpu==1.15.0
rank-bm25==0.2.2
numpy>=2.0

# --- agents and LLM --------------------------------------------------------
groq==0.37.1
crewai==1.15.16
litellm==1.96.2

# --- API, database, auth ---------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0
# PostgreSQL driver (ADR-023). SQLite needs no driver, so this is only used
# when DATABASE_URL points at Postgres.
psycopg[binary]>=3.2
python-jose[cryptography]>=3.3
bcrypt>=4.1

# --- shared ----------------------------------------------------------------
pydantic>=2.7
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.32
